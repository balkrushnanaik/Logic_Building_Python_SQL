def pyramid_star_pattern(rows=5):
    for i in range(1, rows + 1):
        print(' ' * (rows - i) + '*' * (2 * i - 1))

if __name__ == '__main__':
    try:
        n = int(input('Enter number of rows: '))
    except ValueError:
        n = 5
    pyramid_star_pattern(n)
